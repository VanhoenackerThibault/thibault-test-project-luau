--!strict

export type BotState = {
    tweening: boolean,
    autoFarmEnabled: boolean,
    farmRunning: boolean,
    autoFarmPaused: boolean,
    claimedHive: boolean,
    hive: Instance?,
    converting: boolean,
    isConverting: boolean,
    inSelectedField: boolean,
    selectedField: string?,
    walkSpeed: number,
    walkSpeedEnabled: boolean,
    tweenSpeed: number,
    convertPercentage: number,
    loadedScript: boolean,
    playerData: { [string]: any }?,
    startHoney: number?,
    scriptStartTime: number?,
    stopDistance: number,
    convertBalloon: boolean,
    scriptStopped: boolean,
    showDebugInfo: boolean,
    farmPrecision: boolean,
    waitConvertTime: number,
    preciseChains: { [number]: { FEID: number, ID: number, Index: number, Pos: Vector3 }},
    activeBuffs: { any },
    importantTokens: { string },
    selectedPlanters: { string },
    nectarSettings: { [string]: {hours: number, priority: number, enabled: boolean}},
    autoPlanter: boolean,
    activePlanters: { [number]: any },
    blackListedFields: { string },
    collectEveryEnabled: boolean,
    collectEvery: number,
    collectAtPercentageEnabled: boolean,
    collectAtPercentage: number,
    allowedPlanters: number
}

local BotState: BotState = {
    tweening = false,
    autoFarmEnabled = false,
    farmRunning = false,
    autoFarmPaused = false,
    claimedHive = false,
    hive = nil,
    converting = false,
    isConverting = false,
    inSelectedField = false,
    selectedField = nil,
    walkSpeed = 27,
    walkSpeedEnabled = false,
    tweenSpeed = 70,
    convertPercentage = 100,
    loadedScript = false,
    playerData = nil,
    startHoney = nil,
    scriptStartTime = nil,
    stopDistance = 1,
    convertBalloon = true,
    scriptStopped = false,
    showDebugInfo = false,
    farmPrecision = false,
    waitConvertTime = 0,
    preciseChains = {},
    activeBuffs = {},
    importantTokens = {},
    selectedPlanters = {},
    nectarSettings = {
        Invigorating = { enabled = false, hours = 0, priority = 1 },
        Refreshing   = { enabled = false, hours = 0, priority = 1 },
        Satisfying   = { enabled = false, hours = 0, priority = 1 },
        Motivating   = { enabled = false, hours = 0, priority = 1 },
        Comforting   = { enabled = false, hours = 0, priority = 1 },
    },
    autoPlanter = false,
    activePlanters = {},
    blackListedFields = {},
    collectEveryEnabled = false,
    collectEvery = 2,
    collectAtPercentageEnabled = false,
    collectAtPercentage = 100,
    allowedPlanters = 3
}

return BotState